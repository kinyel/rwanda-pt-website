---
/**
 * The one button in the system. Renders <a> when given href, <button> otherwise.
 *
 * The styling is in global.css under `.btn`, so the React contact form renders
 * exactly the same buttons. See that block for how the plate is built and for
 * the contrast note on the brand orange.
 *
 * Variants exist for a reason, not for variety:
 *   primary   the single most important action in a view
 *   primaryInk the same orange plate with an ink label, for small labels where
 *              6.05:1 matters more than the white-on-orange convention
 *   secondary a real alternative to the primary action
 *   ghost     tertiary, used inside dense areas
 *   onDark    primary equivalent for dark surfaces
 *   secondaryOnDark  the real alternative alongside a primary, on dark
 *
 * `secondary` and `secondaryOnDark` are not interchangeable. `secondary` is a
 * white plate, which on a dark ground reads as loud as the primary and splits
 * the eye between two filled buttons. `secondaryOnDark` is an outline in white
 * at 30%: 3.4:1 against ink-950, so the boundary itself clears WCAG 1.4.11,
 * with white label text at 19:1. It stays unmistakably a button while letting
 * the orange primary carry the weight.
 */
interface Props {
  href?: string;
  variant?: 'primary' | 'primaryInk' | 'secondary' | 'ghost' | 'onDark' | 'secondaryOnDark';
  size?: 'sm' | 'md' | 'lg';
  external?: boolean;
  type?: 'button' | 'submit';
  disabled?: boolean;
  class?: string;
  [key: string]: unknown;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  external = false,
  type = 'button',
  disabled = false,
  class: className = '',
  ...rest
} = Astro.props;

/* The look lives in `@layer components` in global.css, not here. The contact
   form is a React island and Astro's scoped styles cannot reach it, so a
   button defined only in this file would have quietly become two designs. */
const cls = `btn btn-${size} btn-${variant} ${className}`.trim();
const externalAttrs = external ? { target: '_blank', rel: 'noopener noreferrer' } : {};
---

{
  href ? (
    <a href={href} class={cls} {...externalAttrs} {...rest}>
      <slot />
    </a>
  ) : (
    <button type={type} class={cls} disabled={disabled} {...rest}>
      <slot />
    </button>
  )
}
